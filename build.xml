<?xml version="1.0" encoding="UTF-8"?>
<!--
  Doctor Appointment System – Standalone Ant Build
  ─────────────────────────────────────────────────
  Targets
    compile  (default) – compile all Java sources
    war      – produce a deployable WAR in dist/
    clean    – remove all generated artefacts

  Dependencies (bundled in lib/)
    lib/mysql-connector-j-9.1.0.jar   – JDBC driver (runtime + compile)
    lib/javax.servlet-api.jar          – Servlet API 3.1 (compile-only)

  No NetBeans or GlassFish installation required.
-->
<project name="Doctor-Appointment-System" default="compile" basedir=".">
    <description>Doctor Appointment System – compile, package and clean.</description>

    <!-- ── Directories ───────────────────────────────────────────── -->
    <property name="src.dir"     value="src/java"/>
    <property name="web.dir"     value="web"/>
    <property name="lib.dir"     value="lib"/>
    <property name="build.dir"   value="build/web"/>
    <property name="classes.dir" value="${build.dir}/WEB-INF/classes"/>
    <property name="dist.dir"    value="dist"/>
    <property name="war.name"    value="Doctor-Appointment-System.war"/>

    <!-- ── Compile classpath ─────────────────────────────────────── -->
    <path id="compile.classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
    </path>

    <!-- ── init ──────────────────────────────────────────────────── -->
    <target name="init">
        <mkdir dir="${classes.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <!-- ── compile ───────────────────────────────────────────────── -->
    <target name="compile" depends="init" description="Compile Java sources">
        <javac srcdir="${src.dir}"
               destdir="${classes.dir}"
               classpathref="compile.classpath"
               source="1.8" target="1.8"
               includeantruntime="false"
               debug="true"
               encoding="UTF-8"/>
        <echo message="[BUILD] Compilation successful → ${classes.dir}"/>
    </target>

    <!-- ── war ───────────────────────────────────────────────────── -->
    <target name="war" depends="compile" description="Package deployable WAR">
        <war destfile="${dist.dir}/${war.name}"
             webxml="${web.dir}/WEB-INF/web.xml">
            <!-- static web resources (HTML, CSS, JSP, WEB-INF) -->
            <fileset dir="${web.dir}"/>
            <!-- JDBC driver must be inside the WAR at runtime -->
            <lib file="${lib.dir}/mysql-connector-j-9.1.0.jar"/>
            <!-- compiled classes -->
            <classes dir="${classes.dir}"/>
        </war>
        <echo message="[BUILD] WAR ready → ${dist.dir}/${war.name}"/>
    </target>

    <!-- ── clean ─────────────────────────────────────────────────── -->
    <target name="clean" description="Remove generated build artefacts">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
        <echo message="[BUILD] Clean complete."/>
    </target>

</project>
