# Database Configuration
# Edit these values to match your MySQL setup
db.url=jdbc:mysql://localhost:3306/medical_appointment_system?useSSL=false
db.user=root
db.password=root

# Admin password (change this before deploying to production)
# To use a stronger password, update this value
admin.password=Admin@1234
